apiVersion: v1
kind: Service
metadata:
  name: opensearch-mcp-server
  namespace: log-correlation
  labels:
    app.kubernetes.io/name: opensearch-mcp-server
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: opensearch-mcp-server
  ports:
    - port: 80
      targetPort: 9900
      protocol: TCP
      name: http
